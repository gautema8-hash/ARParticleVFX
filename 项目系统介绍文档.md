# AR 粒子特效代码平台系统介绍

## 1. 系统概述

AR 粒子特效代码平台是一个面向开发者、设计师和企业用户的 Web 粒子特效平台。系统提供粒子特效浏览、在线预览、WebAR 实景体验、代码复制与导出，并配套用户账号、收藏、会员订阅、订单和管理员后台等业务功能。

系统采用前后端分离架构：

- **前端**：负责页面展示、粒子渲染、摄像头调用、手势识别、代码预览和导出。
- **后端**：负责用户、特效、收藏、订单、会员和后台管理等业务数据。
- **数据库与缓存**：PostgreSQL 保存业务数据，Redis 保存登录 Token 和接口限流数据。

## 2. 系统能解决什么问题

用户无需从零编写粒子动画代码，即可完成以下工作：

1. 浏览不同主题的粒子特效。
2. 在浏览器中实时查看效果。
3. 体验摄像头结合手势的 WebAR 粒子效果。
4. 复制或导出可直接运行的 HTML 特效代码。
5. 通过参数工具快速生成粒子配置。
6. 收藏常用特效，购买单个特效或开通会员。
7. 管理员统一维护用户、订单和特效上架状态。

## 3. 功能模块总览

| 模块 | 主要功能 | 使用对象 |
| --- | --- | --- |
| 首页 | 平台介绍、特效库入口、免费体验入口 | 所有访客 |
| 特效中心 | 按主题和权益筛选特效，查看特效卡片 | 所有访客 |
| 特效详情 | 实时预览、复制代码、导出 HTML/TXT、收藏、购买 | 所有访客/登录用户 |
| WebAR 实景 | 摄像头实时画面、手势驱动粒子、人像粒子体验 | 所有访客 |
| 定价中心 | 免费版、个人 Pro、企业版套餐及单特效购买 | 所有访客/登录用户 |
| 企业服务 | 定制开发、素材授权、API 接入等服务介绍 | 企业客户 |
| 工具箱 | 代码压缩、颜色拾取、参数生成、DIY 粒子预览 | 所有访客 |
| 帮助中心 | 使用教程、授权说明、常见问题 | 所有访客 |
| 用户中心 | 注册、登录、找回密码、会员信息、退出登录 | 普通用户 |
| 我的订单 | 查看订单、查看状态、处理待支付订单 | 登录用户 |
| 管理后台 | 用户、订单、特效管理，特效上架/下架 | 管理员 |

## 4. 前端页面功能说明

### 4.1 首页

首页是系统入口，主要展示平台定位和核心卖点：动态粒子特效、WebAR 体验、一键复制和开箱即用。用户可以从首页进入特效中心或免费体验页面。

路由：`#/`

### 4.2 特效中心

特效中心用于集中展示全部粒子特效。用户可以按照以下条件筛选：

- 全部特效
- AR 专属特效
- 动物粒子
- 花卉粒子
- 几何粒子
- 节日粒子
- 自然粒子
- 科技粒子
- 免费或 Pro 权益

点击特效卡片后进入对应的特效详情页。

路由：`#/effects`

### 4.3 特效详情

详情页是特效的主要使用页面，提供：

- 特效名称、分类、标签和说明。
- 高级 3D 粒子特效的 iframe 实时预览。
- AR 特效的在线体验入口。
- 一键复制完整代码。
- 导出 HTML 文件。
- 导出 TXT 文件。
- 登录后收藏或取消收藏。
- 登录后购买单个特效。

高级 3D 粒子代码会导出为单文件 HTML，使用 Three.js GPU 点云运行；正式商用时建议将 Three.js CDN 替换为企业自有静态资源地址。

路由：`#/effect/:id`

### 4.4 WebAR 实景

WebAR 页面用于体验摄像头与粒子特效结合的效果，不需要安装 App。当前主要包含：

- **宇宙星系**：通过手势控制星系、光带和冲击波。
- **海水潮流**：展示青蓝色潮流粒子效果。
- **人像粒子**：上传照片后重构为粒子人像，可进行爆炸和复原效果。

页面还展示了图像识别扩展方向，该功能当前为预留/后续能力。

路由：`#/ar`

免费体验路由：`#/demo`

使用 WebAR 时需要允许浏览器访问摄像头。摄像头通常只能在 `localhost` 或 HTTPS 环境下使用。

### 4.5 定价中心

定价页面展示三种会员档位：

| 档位 | 价格 | 主要权益 |
| --- | --- | --- |
| 免费版 | ¥0 | 每日 3 次复制/导出，代码带版权标识，仅限学习使用 |
| 个人 Pro | ¥19/月 | 普通特效不限次，AR 每日 10 次，个人商用授权，参数调节 |
| 企业版 | ¥99/月 | 全量特效，AR 无限制，企业商用授权，3 个子账号，定制折扣 |

页面还展示单特效购买说明，当前价格区间为 9.9～49.9 元/个，购买后永久使用。

会员开通流程为：登录 → 创建会员订单 → 完成当前阶段的模拟支付 → 更新会员档位 → 跳转我的订单。

路由：`#/pricing`

### 4.6 企业服务

企业服务用于承接商业合作，当前展示三类服务：

- **定制开发**：品牌 Logo 粒子、产品主题 AR、活动定制粒子等。
- **素材授权**：全库特效商用授权、持续上新和企业级授权。
- **API 接入**：将粒子能力接入企业自己的产品或业务系统。

页面提供联系入口，用于后续商务沟通。

路由：`#/enterprise`

### 4.7 工具箱

工具箱提供四个前端工具：

1. **代码压缩**：删除 CSS/JS 注释和多余空白，输出压缩结果并支持复制。
2. **颜色拾取**：选择颜色并查看 HEX、RGB、HSL 三种格式，支持复制。
3. **参数生成器**：调节粒子数量、颜色、大小、速度和透明度，生成配置代码。
4. **DIY 特效**：输入或修改 HTML，使用沙箱 iframe 实时运行，并支持下载 HTML/TXT。

路由：`#/tools`，子路由示例：`#/tools/compress`、`#/tools/color`、`#/tools/params`、`#/tools/diy`。

### 4.8 帮助中心

帮助中心用于降低使用门槛，包含：

- 使用教程：挑选特效、复制代码、嵌入网页、体验 WebAR。
- 授权说明：不同会员档位的使用范围和限制。
- 常见问题：摄像头权限、浏览器支持、导出代码运行方式等。

路由：`#/help`，子路由示例：`#/help/tutorial`、`#/help/license`、`#/help/faq`。

### 4.9 用户中心

用户中心支持：

- 用户注册。
- 用户登录。
- 用户名、邮箱和密码校验。
- 忘记密码/密码重置。
- 查看当前用户信息和会员档位。
- 查看管理员身份标识。
- 退出登录。

登录成功后，前端将 Token 保存在浏览器本地存储，并在后续请求中通过 `Authorization: Bearer <token>` 发送。

路由：`#/login`

### 4.10 我的订单

我的订单用于查看当前登录用户创建的订单，包括：

- 单特效订单。
- 会员订阅订单。
- 订单号、金额、创建时间。
- 待支付、已支付、已取消、已退款等状态。
- 待支付订单的支付入口。

路由：`#/orders`

## 5. 当前支持的 15 款特效

### AR 特效

| 编码 | 名称 | 说明 |
| --- | --- | --- |
| `galaxy` | 宇宙星系 | 手势驱动螺旋星系、星河光带和引力冲击波 |
| `ocean` | 海水潮流 | 青蓝色潮流粒子随波浪流动 |
| `photoParticle` | 人像粒子 | 上传照片并用约 5 万粒子重构人像 |

### 高级 3D 粒子特效

| 分类 | 特效 |
| --- | --- |
| 动物粒子 | 蝴蝶粒子、游鱼粒子、飞鸟粒子 |
| 花卉粒子 | 花瓣粒子、樱花粒子 |
| 几何粒子 | 科技点阵、粒子波 |
| 节日粒子 | 雪花粒子、烟花粒子 |
| 自然粒子 | 雨滴粒子、萤火虫粒子 |
| 科技粒子 | 星云粒子 |

其中飞鸟、粒子波、烟花和星云标记为 Pro 特效，其余高级 3D 粒子和 AR 特效为免费体验内容。

## 6. 管理后台功能

管理后台仅允许 `role=1` 的管理员访问，包含三个管理页签：

### 用户管理

查看平台用户列表及用户基础信息，用于了解注册用户和会员档位。

### 订单管理

查看全平台订单，包括订单号、订单类型、金额、订单状态和创建时间。

### 特效管理

查看全部特效，并修改特效的上架/下架状态。下架后的特效可以从前台展示中隐藏，但后台仍可查看。

默认管理员账号：

| 用户名 | 密码 | 角色 |
| --- | --- | --- |
| `admin` | `Admin123` | 管理员 |

生产环境部署后应立即修改默认密码，并限制后台访问范围。

## 7. 用户业务流程

### 7.1 浏览并使用免费特效

首页 → 特效中心 → 选择分类 → 进入详情 → 在线预览 → 复制或导出代码。

### 7.2 WebAR 体验

首页/AR 页面 → 免费体验 → 允许摄像头 → 选择 AR 模式 → 使用手势或上传照片进行交互。

### 7.3 收藏特效

注册/登录 → 进入特效详情 → 点击收藏 → 在登录态下保持收藏记录。

### 7.4 购买单个特效

登录 → 特效详情 → 点击购买 → 创建订单 → 模拟支付 → 在我的订单中查看结果。

### 7.5 开通会员

登录 → 定价中心 → 选择个人 Pro 或企业版 → 创建订阅订单 → 模拟支付 → 用户会员档位升级。

## 8. 技术架构和项目目录

```text
ARParticleVFX/
├── ar-particle-vfx-web/          # 前端：Vite + Three.js + MediaPipe
│   ├── src/views/                # 各业务页面
│   ├── src/effects/              # 特效注册表和高级 3D 粒子引擎
│   ├── src/tools/                # 工具箱功能
│   ├── src/camera.js             # 摄像头采集
│   ├── src/handTracker.js        # 手势识别
│   ├── src/particleScene.js      # Three.js AR 粒子场景
│   ├── src/exporter.js           # 代码复制与文件导出
│   └── models/                   # MediaPipe 模型和 WASM 文件
├── ar-particle-vfx-admin/        # 后端：Spring Boot 单体服务
│   ├── controller/               # REST 接口
│   ├── service/                  # 业务服务
│   ├── dao/mapper/               # 数据访问层
│   ├── entity/                   # 数据对象、请求对象、返回对象
│   ├── config/                   # Token、限流、Redis、MyBatis 配置
│   └── sql/                      # 建表、种子数据和管理员脚本
└── 项目系统介绍文档.md           # 本文档
```

主要技术：

- 前端：Vite 5、原生 JavaScript、Three.js 0.160、MediaPipe Tasks Vision、WebGL/GPU 点云、原生 Hash 路由。
- 后端：JDK 8、Spring Boot 2.4.2、MyBatis、PageHelper、PostgreSQL、Redis、Log4j2、BCrypt。

## 9. 后端接口功能分类

| 分类 | 代表接口 | 作用 |
| --- | --- | --- |
| 用户 | `/api/user/register`、`/api/user/login` | 注册、登录、重置密码和用户信息 |
| 收藏 | `/api/user/favorite/{effectCode}` | 收藏、取消收藏和查询收藏 |
| 特效 | `/api/effect/list`、`/api/effect/{id}` | 分页查询特效和查看详情 |
| 订单 | `/api/order/create`、`/api/order/list` | 创建订单和查询个人订单 |
| 支付 | `/api/order/pay/callback` | 当前阶段的模拟支付回调 |
| 后台 | `/api/admin/users`、`/api/admin/orders`、`/api/admin/effects` | 管理员查询和维护业务数据 |

接口统一返回 `{ code, message, data }` 结构。需要登录的接口通过 Token 鉴权，管理员接口额外校验管理员角色。

## 10. 数据和安全说明

- 用户密码使用 BCrypt 加密保存，不保存明文密码。
- 登录 Token 保存在 Redis，默认有效期为 7 天。
- 管理后台接口统一校验管理员角色。
- 收藏、订单和支付操作校验用户归属，防止越权访问。
- 支付状态使用条件更新保证重复回调不会重复处理。
- 会员档位只允许升级，不允许通过支付流程降级。
- 注册、登录、密码重置和支付回调接口具备 Redis 计数限流。
- 前端 DIY 预览使用沙箱 iframe 运行用户输入的 HTML。

## 11. 启动方式

### 11.1 初始化数据库

```bash
psql -U postgres -c "CREATE DATABASE arpfx;"
psql -U postgres -d arpfx -f ar-particle-vfx-admin/sql/schema.sql
psql -U postgres -d arpfx -f ar-particle-vfx-admin/sql/seed.sql
psql -U postgres -d arpfx -f ar-particle-vfx-admin/sql/admin.sql
```

### 11.2 启动后端

```bash
cd ar-particle-vfx-admin
mvn spring-boot:run
```

后端默认地址：`http://localhost:8080`

### 11.3 启动前端

```bash
cd ar-particle-vfx-web
npm install
npm run dev
```

前端默认地址：`http://localhost:5173`。开发环境下 `/api` 请求会代理到后端 `8080` 端口。

## 12. 当前版本边界

- 当前支付流程为模拟支付，正式上线前需要接入微信支付、支付宝或其他真实支付渠道，并完成回调验签。
- WebAR 摄像头功能需要用户授权，并建议在 HTTPS 或 localhost 环境运行。
- 图像识别 AR 入口已经预留，但页面提示该能力后续上线。
- 企业服务页面目前主要用于展示服务内容和承接咨询，实际商务流程需要另行配置。
- 生产环境需要修改数据库、Redis、管理员密码和跨域配置，不能直接使用开发环境默认配置。
