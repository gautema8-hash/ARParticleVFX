# 前后端功能 - 执行计划（后端依赖项 · 分步骤）

> 说明：**纯前端功能已全部实现完毕**（导航 / 路由 / 8 页面 / 特效中心 / 详情导出 / AR / 定价 / 企业服务 / 工具箱 / 帮助中心 / 移动端 / SEO 标题）。本文档列出**需后端支撑**的功能，按依赖顺序排列，供后续逐步实现。

## 〇、纯前端已完成清单（回顾）

| 模块 | 状态 |
| --- | --- |
| 顶部导航 + hash 路由 + 8 页面视图 | ✅ |
| 特效注册表 registry + 特效中心 + 详情页 | ✅ |
| exporter 一键复制 / 单文件导出 | ✅ |
| WebAR 专题页 + 免费体验 | ✅ |
| 定价页 + 企业服务页 + 工具箱 + 帮助中心 | ✅ |
| 移动端适配 + 页面 SEO 标题 | ✅ |

## 一、后端依赖功能总览

| 步骤 | 功能 | 前后端 | 优先级 | 依赖 |
| --- | --- | --- | --- | --- |
| A | BaaS 接入（注册/登录/收藏） | 前 + 后 | 高 | 无 |
| B | 单特效支付 | 前 + 后 | 高 | A + 企业主体 |
| C | 会员体系 + 订阅支付 | 前 + 后 | 高 | B |
| D | 特效模板量产（30→100 款） | 纯前端（内容） | 中 | A（上架） |
| E | 创作者平台 | 前 + 后 | 中 | A + C |
| F | 开放 API | 后为主 | 中 | D |
| G | 定制服务后端（订单/交付） | 前 + 后 | 低 | B |

## 步骤 A：BaaS 接入（Supabase 注册/登录/收藏）

**目标**：用户注册登录 + 收藏 + 已购记录。

**前端**：
- `npm install @supabase/supabase-js`
- 新建 `src/supabase.js`（初始化 client、auth 封装）
- 登录/注册 UI（导航「登录」→ 登录页或弹窗）
- 详情页「收藏」按钮接入 `favorites` 表
- 会员态写入 `profiles` 表

**后端/BaaS**：
- Supabase 建项目 + 建表：`profiles(id, email, tier)`、`favorites(user_id, effect_id)`、`orders(...)`
- 开启 Auth（邮箱 / 第三方登录）

**验收点**：能注册登录、收藏/取消收藏，刷新后仍在。

**可行性**：✅ 高（BaaS 免自建后端）。国内需确认 supabase 可达性，必要时换 LeanCloud / 自建。

## 步骤 B：单特效支付

**目标**：微信/支付宝购买单特效。

**前端**：详情页「购买」→ 下单 → 拉起支付 → 回调解锁。

**后端**：
- 企业主体 + 商户号 + ICP 备案
- 下单接口、支付回调验签、订单落库、授权下发
- （BaaS Edge Functions 或自建 Node/Nest）

**验收点**：完成测试支付后特效解锁，订单/授权可查。

**可行性**：⚠️ 中。需企业资质 + 数日审核；是首个硬外部依赖。

## 步骤 C：会员体系 + 订阅支付

**目标**：免费/Pro/企业三档 + 订阅（月/年）。

**前端**：定价页「立即开通」→ 订阅流程；会员中心页。

**后端**：订阅创建、续费、退款、回调、权限中间层（免费/Pro/企业）。

**验收点**：开通会员后额度/版权标识按档位生效（前端 `buildSingleFileHTML` 的 `opts.isMember` 已就绪）。

**可行性**：⚠️ 中。订阅比单购复杂。

## 步骤 D：特效模板量产（30→100 款，纯前端内容）

**目标**：把 registry 里 `mode:null` 的占位特效做成真实可运行模板。

**前端**：
- 在 `particleScene.js` 或 `effects/` 中为每个特效实现「参数配置 + 更新函数」
- 动物/花卉/几何/节日/自然/科技各 10+ 款
- 每款生成封面图（canvas 截帧）

**验收点**：`listEffects()` 达 30/100 款，均可预览 + 导出。

**可行性**：✅ 高（纯前端，复用 makePoints 引擎）；工作量在素材生产。

## 步骤 E：创作者平台

**目标**：开发者上传原创特效，平台分成。

**前后端**：上传审核 → 打包规范 → 收益分成结算。

**验收点**：外部创作者可上传并上架特效。

**可行性**：⚠️ 中。需审核/结算/版权合规体系。

## 步骤 F：开放 API

**目标**：对接第三方建站/H5 工具，按调用量计费。

**后端**：REST API + 鉴权 + 计费 + 配额。

**验收点**：第三方可调用特效渲染 API。

**可行性**：⚠️ 中。

## 步骤 G：定制服务后端

**目标**：企业定制订单管理 + 交付。

**前后端**：订单系统、需求沟通、交付与验收流程。

**验收点**：能下单并跟踪企业定制单。

**可行性**：⚠️ 中（当前 mailto 表单可先用，成交依赖商务）。

## 二、推荐执行顺序

```
A(BaaS) → B(单特效支付) → C(会员订阅) → D(特效量产) → E(创作者) → F(API) → G(定制后端)
```

> 其中 **A→B→C 是商业化主干**（先能收钱）；**D 是供给扩充**（纯前端，可随时穿插）；**E/F/G 是生态放大**。
