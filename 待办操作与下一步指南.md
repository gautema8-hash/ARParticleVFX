# AR 粒子特效平台 · 待办操作与下一步指南

> 生成日期：2026-08-14
> 本文件基于**对当前机器环境的实测**编写，所有路径、端口、密码均以实测为准。

---

## 一、环境现状（已实测）

| 组件 | 状态 | 实际位置 / 版本 | 端口 |
| --- | --- | --- | --- |
| PostgreSQL | ✅ 运行中 | `D:\sofa\pg\bin\psql.exe`（PostgreSQL 14.23） | 5432 |
| Redis | ✅ 运行中 | `D:\sofa\redis\redis\redis-cli.exe`（无密码） | 6379 |
| 数据库 `arpfx` | ❌ **未创建** | 现有库：`postgres` / `template0` / `template1` / `xingzhouerp` | — |
| 后端服务 | ⏳ 未启动 | Spring Boot 2.4.2（Maven 3.3.9） | 8080 |
| 前端服务 | ⏳ 未启动 | Vite 5（Node 位于 `D:\sofa\qianduan\nodejs`） | 5173 |

⚠️ **注意**：`psql`、`redis-cli` 都不在系统 PATH 中，下面所有命令**必须使用完整路径**（已写好在命令里，直接复制即可）。

---

## 二、端口与服务清单

| 端口 | 用途 | 备注 |
| --- | --- | --- |
| 5432 | PostgreSQL | 已在运行，无需操作 |
| 6379 | Redis | 已在运行，无需操作 |
| 8080 | 后端 API | 需启动（`mvn spring-boot:run`） |
| 5173 | 前端开发服务器 | 需启动（`npm run dev`），`/api` 已代理到 8080 |

---

## 三、待操作步骤（按顺序执行）

### 步骤 1：初始化数据库（一次性）

数据库 `arpfx` 尚未创建，需要执行建库 + 建表 + 导种子数据。

```powershell
# 设置密码（当前实测默认密码为 postgres）
$env:PGPASSWORD='postgres'

# 1) 创建数据库
& 'D:\sofa\pg\bin\psql.exe' -U postgres -h 127.0.0.1 -c "CREATE DATABASE arpfx;"

# 2) 建表 + 索引 + 外键
& 'D:\sofa\pg\bin\psql.exe' -U postgres -h 127.0.0.1 -d arpfx -f 'd:\sofa\aiproject\ARParticleVFX\ar-particle-vfx-admin\sql\schema.sql'

# 3) 导入 15 个特效种子数据（effect_code 与前端特效 id 对齐）
& 'D:\sofa\pg\bin\psql.exe' -U postgres -h 127.0.0.1 -d arpfx -f 'd:\sofa\aiproject\ARParticleVFX\ar-particle-vfx-admin\sql\seed.sql'
```

**验证建表成功**：

```powershell
& 'D:\sofa\pg\bin\psql.exe' -U postgres -h 127.0.0.1 -d arpfx -c "\dt"
# 应看到 4 张表：sys_user / biz_effect / biz_favorite / biz_order
```

**验证种子数据**：

```powershell
& 'D:\sofa\pg\bin\psql.exe' -U postgres -h 127.0.0.1 -d arpfx -c "SELECT effect_code, effect_name, tier FROM biz_effect ORDER BY id;"
# 应看到 15 条记录（galaxy、ocean、photoParticle、butterfly ... nebula）
```

> 如果数据库密码不是 `postgres`，请把命令里的 `$env:PGPASSWORD='postgres'` 改成实际密码，
> 并同步修改 `ar-particle-vfx-admin\src\main\resources\config\application-dev.yml` 中的
> `password: ${DB_PASSWORD:postgres}`（可用环境变量 `DB_PASSWORD` 覆盖）。

---

### 步骤 2：启动后端

```powershell
cd d:\sofa\aiproject\ARParticleVFX\ar-particle-vfx-admin
mvn spring-boot:run
```

后端配置（`application-dev.yml`）已与实测环境一致：
- 数据库：`jdbc:postgresql://127.0.0.1:5432/arpfx`，用户 `postgres`，密码 `postgres`
- Redis：`127.0.0.1:6379`，无密码

**验证后端启动成功**（浏览器或命令行访问，游客可访问、无需登录）：

```powershell
curl.exe http://localhost:8080/api/effect/list
# 返回 JSON：{"code":200,"data":{"total":15,"list":[...]},...}
```

---

### 步骤 3：启动前端

```powershell
cd d:\sofa\aiproject\ARParticleVFX\ar-particle-vfx-web
npm install    # 若 node_modules 已存在可跳过
npm run dev
```

浏览器打开 **http://localhost:5173**

> 前端已配置 vite 代理：`/api` → `http://localhost:8080`，所以前端请求无需处理跨域。

---

## 四、支付回调说明

**当前状态：模拟支付回调**（接口：`POST /api/order/pay/callback?orderNo=xxx`）。
本轮已加**订单归属校验**（仅订单本人可触发）+ **幂等 CAS**（防重复处理），但**没有真实扣款**。

**接入真实支付需要提供/办理（非代码，属外部依赖）：**

| 事项 | 说明 |
| --- | --- |
| 企业主体 + ICP 备案 | 国内经营网站/收款的硬门槛 |
| 微信支付商户号 | 需企业资质、对公账户，审核数日 |
| 支付宝商户号 | 同上 |
| 支付 SDK 密钥（AppID/MchID/API Key/证书） | 用于下单签名与回调验签 |

**代码侧切换真实支付的改造点（后续）**：
1. `OrderController` 新增真实下单接口（调用微信/支付宝统一下单）。
2. 新增支付回调接口，做**签名验签 + 幂等 + 金额校验**。
3. 删除 `mockPayCallback`（当前仅用于联调）。
4. `OrderServiceImpl` 的会员升级逻辑已就绪，可直接复用。

---

## 五、端到端验证清单

### 5.1 游客浏览（无需登录）
- [ ] 打开 http://localhost:5173 能显示首页
- [ ] 进入「特效中心」能看到分类与特效卡片
- [ ] 访问 http://localhost:8080/api/effect/list 返回 15 条特效

### 5.2 用户体系（前端页面）
- [ ] 点右上角「登录」→ 能进入登录页（不再是死链接）
- [ ] 切换到「注册」，填用户名（3-32 位）+ 密码（6-32 位）能注册
- [ ] 用刚注册的账号登录 → 右上角显示昵称
- [ ] 进入登录页能看到「会员档位：免费版」和「退出登录」

### 5.3 收藏（前端页面）
- [ ] 进入任意特效详情页 → 点「收藏」→ 提示「已收藏」，按钮变「取消收藏」
- [ ] 刷新页面后收藏状态仍在（调用了后端）

### 5.4 免费体验 AR（前端页面）
- [ ] 点「免费体验」进入 `#/demo` 才弹出摄像头授权（**打开首页不应再请求摄像头**）
- [ ] 授权后伸右手能触发星系 / 光带 / 冲击波

### 5.5 订单 + 会员（用命令行，前端订单 UI 尚未接）

```powershell
# 1) 登录拿 token（用你刚注册的账号）
curl.exe -X POST http://localhost:8080/api/user/login -H "Content-Type: application/json" -d "{\"username\":\"test\",\"password\":\"123456\"}"
# 记下返回里的 token

# 2) 下单：会员订阅（orderType=1, tier=1 个人Pro）
curl.exe -X POST http://localhost:8080/api/order/create -H "Authorization: Bearer <token>" -H "Content-Type: application/json" -d "{\"orderType\":1,\"tier\":1}"
# 记下返回里的 orderNo

# 3) 模拟支付回调（仅订单本人可触发）
curl.exe -X POST "http://localhost:8080/api/order/pay/callback?orderNo=<orderNo>" -H "Authorization: Bearer <token>"

# 4) 查看用户信息，确认档位已升级为 1（个人Pro）
curl.exe http://localhost:8080/api/user/info -H "Authorization: Bearer <token>"
```

**验证点**：
- [ ] 下单成功返回订单号
- [ ] 回调后订单状态变为「已支付」
- [ ] 用户 `tier` 从 0 升级为 1
- [ ] 重复回调同一订单返回「订单状态异常」（幂等生效）
- [ ] 用他人 token 回调你的订单返回「无权限」（越权防护生效）

---

## 六、下一步计划（建议顺序）

| 优先级 | 事项 | 类型 | 说明 |
| --- | --- | --- | --- |
| P0 | 跑通上述 1-5 步 | 操作 | 先把当前代码跑起来，验证修复效果 |
| P0 | 替换版权音乐 | 资源 | `zhoujielunwith.ogg` 商用有版权风险，务必换掉 |
| P1 | 前端订单/支付 UI | 开发 | pricing「立即开通」、详情「购买」对接下单接口 |
| P1 | 接口限流/防刷 | 开发 | 登录/注册/回调加 Redis 计数器限流 |
| P1 | 特效目录后端化 | 开发 | 前端特效中心从 `/api/effect/list` 拉取（当前渲染仍用本地 registry） |
| P1 | 导出真实 AR 特效代码 | 开发 | 当前 `exporter.js` 导出的是占位 demo，需实现真实模板 |
| P2 | 接入真实支付 | 外部+开发 | 企业主体 + 商户号 + 回调验签 |
| P2 | 生产部署 | 运维 | Nginx 反代 + HTTPS + CORS 收敛到具体域名 |

---

## 七、对当前项目的意见（结论）

1. **技术底子好**：后端分层规范、注释充分；前端 Vite 工程化 + Three.js/MediaPipe 的 AR 能力是真实可用的核心资产。
2. **本轮已完成的修复**：P0/P1 安全漏洞（越权回调、MD5→BCrypt、防用户名枚举、Integer 引用比较、幂等、会员仅升不降、UUID 订单号、Jedis 升级）已全部修复；前后端用户体系 + 收藏链路已真实打通；摄像头改为按需启动。
3. **当前最大的缺口**：**真实支付**（需企业资质，非代码可解决）和**特效内容量产**（12 款中 9 款仍是占位）。这是决定能否商用的关键。
4. **商业定位建议**：不要指望纯 C 端 19 元订阅跑量；更现实的是「**免费工具引流 + 企业定制/API 授权**」双轮驱动，先把「定制开发 + 素材授权 + API 接入」三个高客单价场景做实。
5. **风险提示**：版权音乐、宣传口径（首页"200+ 款"与实际的 15 款不符）、支付安全，是三个最可能"翻车"的点，务必在对外前解决。

---

## 附：常用命令速查

```powershell
# psql 完整路径
& 'D:\sofa\pg\bin\psql.exe' -U postgres -h 127.0.0.1 -d arpfx

# redis-cli 完整路径
& 'D:\sofa\redis\redis\redis-cli.exe' -h 127.0.0.1 -p 6379

# 检查端口占用
netstat -ano | findstr :8080
```
