<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.arpfx.platform.dao.mapper.KnowledgeMapper">
<sql id="cols">id,title,category,summary,content,image_base64,status,view_count,like_count,favorite_count,(SELECT nickname FROM sys_user u WHERE u.id=create_by) AS author_name,create_time,update_time,create_by,is_deleted</sql>
<select id="selectPublished" resultType="com.arpfx.platform.entity.po.BizKnowledge">SELECT <include refid="cols"/> FROM biz_knowledge WHERE is_deleted=0 AND status=1 ORDER BY id DESC</select>
<select id="selectAll" resultType="com.arpfx.platform.entity.po.BizKnowledge">SELECT <include refid="cols"/> FROM biz_knowledge WHERE is_deleted=0 <if test="title != null and title != ''">AND title LIKE CONCAT('%',#{title},'%')</if><if test="category != null and category != ''">AND category=#{category}</if><if test="status != null">AND status=#{status}</if> ORDER BY id ASC</select>
<select id="selectById" resultType="com.arpfx.platform.entity.po.BizKnowledge">SELECT <include refid="cols"/> FROM biz_knowledge WHERE id=#{id} AND is_deleted=0</select>
<insert id="insert" parameterType="com.arpfx.platform.entity.po.BizKnowledge" useGeneratedKeys="true" keyProperty="id">INSERT INTO biz_knowledge(title,category,summary,content,image_base64,status,create_by) VALUES(#{title},#{category},#{summary},#{content},#{imageBase64},#{status},#{createBy})</insert>
<update id="update" parameterType="com.arpfx.platform.entity.po.BizKnowledge">UPDATE biz_knowledge SET title=#{title},category=#{category},summary=#{summary},content=#{content},image_base64=#{imageBase64},status=#{status},update_time=NOW() WHERE id=#{id} AND is_deleted=0</update>
<update id="deleteLogical">UPDATE biz_knowledge SET is_deleted=1,update_time=NOW() WHERE id=#{id} AND is_deleted=0</update>
<update id="incrementView">UPDATE biz_knowledge SET view_count=COALESCE(view_count,0)+1 WHERE id=#{id} AND is_deleted=0</update>
<update id="incrementFavorite">UPDATE biz_knowledge SET favorite_count=COALESCE(favorite_count,0)+1 WHERE id=#{id} AND is_deleted=0</update>
<update id="decrementFavorite">UPDATE biz_knowledge SET favorite_count=GREATEST(COALESCE(favorite_count,0)-1,0) WHERE id=#{id} AND is_deleted=0</update>
</mapper>
