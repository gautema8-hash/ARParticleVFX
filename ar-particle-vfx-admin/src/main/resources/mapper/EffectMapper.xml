<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arpfx.platform.dao.mapper.EffectMapper">

    <sql id="Base_Column_List">
        id, effect_code, effect_name, category, mode, tags, tier, description,
        cover_url, price, status, create_time, update_time, create_by, update_by, is_deleted
    </sql>

    <select id="selectPage" resultType="com.arpfx.platform.entity.po.BizEffect">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_effect
        WHERE is_deleted = 0 AND status = 1
        <if test="category != null and category != '' and category != 'all'">
            AND category = #{category}
        </if>
        <if test="tier != null">
            AND tier = #{tier}
        </if>
        ORDER BY id DESC
    </select>

    <select id="selectById" resultType="com.arpfx.platform.entity.po.BizEffect">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_effect
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByCode" resultType="com.arpfx.platform.entity.po.BizEffect">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_effect
        WHERE effect_code = #{effectCode} AND is_deleted = 0
    </select>

    <insert id="insert" parameterType="com.arpfx.platform.entity.po.BizEffect" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_effect (effect_code, effect_name, category, mode, tags, tier, description, cover_url, price, status)
        VALUES (#{effectCode}, #{effectName}, #{category}, #{mode}, #{tags}, #{tier}, #{description}, #{coverUrl}, #{price}, #{status})
    </insert>

    <select id="selectAll" resultType="com.arpfx.platform.entity.po.BizEffect">
        SELECT <include refid="Base_Column_List"/>
        FROM biz_effect
        WHERE is_deleted = 0
        ORDER BY id DESC
    </select>

    <update id="updateStatus">
        UPDATE biz_effect SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>
